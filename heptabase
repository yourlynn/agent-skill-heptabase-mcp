#!/bin/bash
# Heptabase CLI Wrapper
# 簡化 uv run 呼叫

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

cd "$SCRIPT_DIR"
exec uv run --with click --with mcp --with httpx \
  scripts/heptabase.py "$@"
